services:
  - type: web
    name: coach-fred
    runtime: static
    # No build step: it's a single self-contained index.html
    buildCommand: ""
    staticPublishPath: .
    pullRequestPreviewsEnabled: true
    headers:
      - path: /*
        name: Cache-Control
        value: "no-cache"
